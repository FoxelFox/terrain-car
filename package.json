{
  "name": "terrain-car",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "bun build src/index.html --outdir=dist --watch --sourcemap --target browser --chunk-naming [name].[ext]",
    "production": "bun build --production src/index.html --outdir=dist --target browser --chunk-naming [name].[ext]"
  },
  "dependencies": {
    "@babylonjs/core": "^8.3.1",
    "@babylonjs/havok": "^1.3.10",
    "@babylonjs/inspector": "^8.3.1",
    "@babylonjs/materials": "^8.3.1"
  }
}
